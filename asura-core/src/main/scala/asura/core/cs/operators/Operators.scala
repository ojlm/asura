package asura.core.cs.operators

object Operators {

  // comparison
  val EQ = "$eq"
  val NE = "$ne"
  val GT = "$gt"
  val GTE = "$gte"
  val LT = "$lt"
  val LTE = "$lte"
  val IN = "$in"
  val NIN = "$nin"
  val REGEX = "$regex"

  // logical
  val AND = "$and"
  val NOT = "$not"
  val NOR = "$nor"
  val OR = "$or"

  // element
  val TYPE = "$type"

  // array
  val SIZE = "$size"

  // script
  val SCRIPT = "$script"
}
