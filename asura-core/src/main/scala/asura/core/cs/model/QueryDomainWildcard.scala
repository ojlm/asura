package asura.core.cs.model

case class QueryDomainWildcard(
                                var date: String,
                                val domain: String,
                              ) extends QueryPage
